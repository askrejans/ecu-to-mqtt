# Link "Generic Dash 2" (Race Technology Dash2Pro) stream: four consecutive IDs
# from the base. Byte order and scaling differ from Generic Dash — selecting the
# wrong one produces plausible but incorrect values.
ecu_protocol = "link_generic_dash2"
connection_type = "tcp"
tcp_host = "127.0.0.1"
tcp_port = 29536
can_base_id = 1000
mqtt_host = "localhost"
mqtt_port = 1883
mqtt_base_topic = "/g86/car-1/ecu/"
