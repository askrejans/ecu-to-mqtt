# MegaSquirt dash broadcast read directly from a Linux CAN interface.
#   sudo ip link set can0 up type can bitrate 500000
ecu_protocol = "megasquirt_can_dash"
connection_type = "can"
can_interface = "can0"
can_base_id = 1512
mqtt_host = "localhost"
mqtt_port = 1883
mqtt_base_topic = "/ECU/"
